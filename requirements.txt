fastapi>=0.110
uvicorn[standard]>=0.27
pycryptodome>=3.19
pycryptodomex>=3.19
httpx>=0.27
paho-mqtt>=2.0
python-kasa>=0.10
# tzdata is needed because python-kasa parses device-reported timezone
# strings (e.g. "PST8PDT") that require the IANA tz database; the
# python:3.11-slim image doesn't ship it system-wide. Without this, all
# Kasa connections fail with ZoneInfoNotFoundError before auth even starts.
tzdata>=2024.1
# Optional: powers the smart-charge "Claude narration" toggle (1-2 sentence
# explanation of each decision, attached to the persisted history). The
# rest of the app works fine without an API key — narration only runs
# when the user explicitly enables it AND has saved a key.
anthropic>=0.40
openai>=1.40
